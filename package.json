{
  "name": "harp",
  "version": "1.0.0",
  "description": "A playable harp instrument that is accessible to disabled musicians",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./dist/* && mkdir ./dist/css && cp ./index.html ./dist/index.html && cp ./css/main.css ./dist/css/main.css && esbuild index.js --bundle  --outfile=./dist/bundle.js --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gawainhewitt/harp.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/gawainhewitt/harp/issues"
  },
  "homepage": "https://github.com/gawainhewitt/harp#readme",
  "dependencies": {
    "browserify": "^17.0.0",
    "esbuild": "^0.14.43"
  }
}
